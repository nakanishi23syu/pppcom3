# 実務環境再現 提案書（たたき台・未実装）

> 本ドキュメントは方針検討用の提案書です。この時点では**まだ何も実装しません**。
> ヒアリング内容をもとに「無料でどこまで再現できるか」を整理し、進め方の選択肢を提示することが目的です。

## 1. 背景・目的

- `dicom-tool`（Vue3実装）は元々「これからアサインされる実務プロジェクトの学習」を目的に作られたもの。
- その後、実際に実務プロジェクトへのアサインが確定し、状況が変わった。
- 今後は学習効率を上げるため、このPC上に**実務プロジェクトの構成になるべく近い環境**を用意したい。
- `dicom-tool-2` は `dicom-tool` のフロントエンドを Nuxt / Blazor で再実装した派生プロジェクトであり、今回の環境再現もこの `dicom-tool-2` を土台に検討する。

## 2. 現状（As-Is）の整理

| プロジェクト | 内容 |
|---|---|
| `dicom-tool` | Vue3 + TypeScript フロントエンド、C# / HotChocolate(GraphQL) + EF Core + **SQLite** バックエンド。単一プロセス・単一DBのモノレポ。DICOMアップロードは `uploadDicomFiles` というGraphQL Mutation（**HTTPのマルチパート**送信）で行っている。 |
| `dicom-tool-2` | `dicom-tool` の派生。`nuxt/`（Nuxt3）と `blazor/`（Blazor WebAssembly）の2フロント実装を持つが、**バックエンドは `dicom-tool/backend` の同一GraphQL APIを共有**しており、実務のようなサービス分割・DB分割・非同期連携・DICOM通信は未対応。 |

現状は「VMなし・SQLite・単一バックエンド・常駐アプリなし・Temporalなし・DICOM通信なし（HTTPマルチパートのみ）」という、実務環境とはかなり異なる単純構成。

## 3. 目指す実務環境（To-Be）の要素分解

ヒアリングを重ねて判明した内容を要素ごとに整理すると以下の構成です。

| コンポーネント | 役割 | 実務での想定技術 | dicom-tool-2 での現状 |
|---|---|---|---|
| VM基盤 | Webサーバー・DBのホスト（**Windows Server 2025**、IP `192.168.93.128`、**メモリ4GB**割り当て・他は初期値） | VMware上のVM | なし（このPC上に直接） |
| DB | 検査・シリーズ等のレコード管理 | PostgreSQL（**VM上に導入済み**） | SQLite |
| Webサーバー | 上記VM上で各サービスを公開 | **IIS** | なし |
| ワークリストアプリ | テーブル形式で患者レコードを表示、右クリックでタイムライン起動を指示 | Nuxt | `nuxt/` が近いが単体動作 |
| タイムラインアプリ | ワークリストからの右クリックで開かれる、タイムライン表示 | **Blazor（確定）** | `blazor/` が近いが単体動作 |
| Viewerアプリ | 画像の表示・拡大縮小 | **Nuxt（確定）**。実務プロジェクトでの改修頻度が低く、既存実装をほぼそのまま流用できる想定 | 独立コンポーネントとしては未整理（`nuxt/`・`blazor/`双方に画像ビューア相当の機能あり） |
| 常駐アプリ | ワークリストからの起動命令を**HTTP**で受け取り、タイムラインを起動する仲介役（Discord/LINEのようなタスクトレイ常駐型） | **C#（.NET）**。言語はコスパ（学習コストと実装効率のバランス）重視でC#を希望 | なし |
| サービス間連携 | アップロード・削除時の「ストレージ操作」「DB操作」タスクを中継 | **Temporal workflow（C# SDK、費用のかからないセルフホスト運用を希望）** | なし |
| **DICOM通信（新規）** | 画像の送受信をDICOM標準プロトコルで行う（詳細は4-7） | **DICOM C-ECHO / C-STORE（DIMSE）、AEタイトルによるSCU/SCP双方対応（PACS的機能）** | なし（現状はGraphQLのHTTPマルチパートのみ） |
| APIデバッグ | バックエンドAPIの動作確認・デバッグ | Swagger（対象サービスは**特にこだわりなし**） | なし（GraphQL側はNitro/BananaCakePopを使用中） |

> **VM/Webサーバー関連は確定**：Windows Server 2025・IP `192.168.93.128`・メモリ4GB（他初期値）・PostgreSQL導入済み・WebサーバーはIIS採用。フェーズ1の残作業はIISセットアップとdicom-tool側の接続切り替えのみ。
>
> **アプリ間の起動フロー（確定）**：ワークリストアプリ（Nuxt）でレコードを右クリック→常駐アプリ（C#、HTTP常時待受）が命令を受信→タイムラインアプリ（Blazor）を起動。
>
> **要確認（Viewerの位置づけ）**：Viewerの技術がNuxtであることは確定したが、Timelineアプリとの起動関係（Timelineの中からViewerを開くのか、常駐アプリが個別に起動できるのか）はまだ明言がないため、引き続き7章の確認事項とする。

## 4. 「無料でどこまでできるか」評価

### 4-1. VMware VM
- **VMware Workstation Pro / Fusion Pro は2024年11月以降、個人利用であればライセンスキー不要・完全無料**（機能制限なし）[^1][^2]。商用・組織利用は別条件のため、学習目的の個人利用である前提が必要。
- → **VM基盤（ハイパーバイザー）そのものはコスト0円で構築可能。**
- ただし**ゲストOSとして使うWindows Server 2025は別ライセンス**。無料の評価版（Evaluation Edition）は導入から180日間無料で、`slmgr /rearm` により最大6回再アーム（延長）できるため、**実質約3年間は無料で使い続けられる**[^4]。恒久的に使う場合は別途ライセンス購入が必要だが、学習目的の期間限定利用であれば評価版で十分。
- 現状、**VM構築・PostgreSQL導入は既に完了済み**、メモリはデフォルト2GBから**4GBに増強済み**、それ以外は初期値とのこと。DICOM画像やPostgreSQLのデータ量次第では、後々メモリ／ディスクの追加増強が必要になる可能性がある点は留意。
- **Webサーバーは IIS を採用**とのこと。Windows Server 2025には「サーバーの役割」から無料で追加インストール可能（Windows Serverのライセンスに含まれる機能のため追加費用なし）。ASP.NET Core製アプリ（GraphQL API・DICOM SCP待受プロセス等）をIISでホストする場合は `ASP.NET Core Module (ANCM)` の導入が必要（これも無料）。

### 4-2. PostgreSQL（SQLiteからの移行）
- PostgreSQL本体、EF Core用 `Npgsql.EntityFrameworkCore.PostgreSQL` プロバイダともに無料OSS。
- **VM上には既にPostgreSQLが導入済み**とのことなので、追加のインストール作業は不要。残るのは dicom-tool 側で EF CoreのDBプロバイダをSqliteからNpgsqlに差し替え、接続先を `192.168.93.128` のPostgreSQLに向けるマイグレーション作業。
- **コストは0円**だが、DBプロバイダ変更に伴うマイグレーションの作り直しと型・制約差分の吸収という**作業コスト**は発生する。

### 4-3. ワークリスト／タイムライン／Viewer のサービス分割
- Nuxt・Blazor・Node.js・.NET SDKいずれも無料OSS。技術ライセンス上の障壁はなし。
- 現状の `dicom-tool-2` は「共通バックエンドを叩く2つのフロント実装（nuxt/・blazor/）」であり、実務のような**3つに分かれた独立したマイクロサービス**（ワークリスト＝Nuxt／タイムライン＝Blazor／Viewer＝Nuxt）ではない。実務に近づけるなら、3つを別プロセス・別ポートで独立稼働させ、バックエンドも役割ごとに分割するかを設計する必要がある（現状は `dicom-tool/backend` の単一GraphQL APIに依存）。
- **Viewerについて**：実務プロジェクトでの改修頻度は低いとのことなので、このアプリだけは新規に作り込まず、**既存の `nuxt/` にある画像ビューア相当の実装をほぼそのまま切り出して流用する**方針でコストを抑えられる。

### 4-4. 常駐トレイアプリ
- **言語はC#、命令伝達方式はHTTPと確定**。.NET（WinForms/WPF）の `NotifyIcon` を使ったタスクトレイ常駐アプリが、起動時に `localhost` の軽量HTTPサーバー（例: `ASP.NET Core Minimal API` や `HttpListener`）を立てて常時待受し、ワークリスト（ブラウザ上のNuxt）からのfetchで命令を受け取る構成が最有力。
- Visual Studio Community・.NET SDKともに無料であり、**費用は0円**で実現可能。

### 4-5. Temporal workflow
- ワークフローの実装言語は**C#（.NETのTemporal SDK）**、運用形態は**「お金のかからない方＝セルフホスト」を希望**と確定。
- **セルフホストOSS版はMITライセンスで無料**[^3]。ただし本番相当の構成（Cassandra/Elasticsearchバックエンド）は運用が重く「docker-composeで立てて放置」というわけにはいかない。学習・開発用途であれば、公式CLIが提供する **`temporal server start-dev`**（SQLite内蔵の単一バイナリ開発用サーバー）で十分に検証可能。これなら**無料かつセットアップも軽量**。
- （参考）Temporal Cloudには本番向けの無料枠は無く、Essentialsプランで月額$100〜のため、今回の希望（無料）とは合わない[^3]。
- **管理したいワークフローの範囲が確定**：
  - **アップロード時**：①DICOM画像をストレージへ保存するタスク、②DB（PostgreSQL）へレコード登録するタスクの2つに分離。
  - **削除時**：①ストレージから画像を削除するタスク、②DBからレコードを削除するタスクの2つに分離。
  - アップロード・削除いずれも「ストレージ操作」と「DB操作」を分離し、Temporalが両者の整合性（成功/失敗の連携、リトライ）を仲介する設計。

### 4-6. Swagger（APIデバッグ）
- ASP.NET Core向けの `Swashbuckle.AspNetCore` や `NSwag` はいずれも無料OSSで、OpenAPI仕様書とSwagger UIを自動生成できる。
- 導入対象のAPIは**特にこだわりなし**とのことなので、常駐アプリのローカルHTTP APIや、後述のDICOM SCU/SCPまわりの管理用APIなど、REST系のエンドポイントがあるサービスに適宜導入する方針でよい。
- **費用は0円**で導入可能。

### 4-7. DICOM通信プロトコル化（SCU/SCP対応・PACS化）【新規・重要】

現状 `dicom-tool` は DICOM画像を **HTTP/GraphQLのマルチパート送信**（`uploadDicomFiles` Mutation）でアップロードしているが、これを**DICOM標準のネットワークプロトコル（DIMSE）**に置き換えたいという要望。具体的には：

- **C-ECHO**（疎通確認、いわゆるDICOM Ping）
- **C-STORE**（画像本体の送信）
- **AEタイトル**を用いたアソシエーション（接続）確立

を実装し、システム全体として **SCU（Service Class User＝送信側/クライアント）と SCP（Service Class Provider＝受信側/サーバー）の両方の機能を持つ、簡易PACS**にしたい、という要件と理解しています。

**実現方法（無料での可否）**

- .NET/C#向けの標準的なDICOM通信ライブラリとして **fo-dicom**（Fellow Oak DICOM）がある。C-ECHO・C-STOREを含むDIMSEサービスのSCU/SCP実装、アソシエーション管理、AEタイトル処理を一通りサポートする、活発にメンテナンスされているOSS。
- ライセンスは **MS-PL（Microsoft Public License）**。MITと同様の緩やかなOSSライセンスで、商用・個人問わず無料で利用可能[^5]。
- → **費用面では0円で実現可能**。ただし、これは今回の要件の中でも**技術的難易度が最も高い部分**（DICOM Upper Layer Protocolの理解、アソシエーション/プレゼンテーションコンテキスト/転送構文のネゴシエーション、SCP側の常駐リスナー実装等）であり、時間コストは大きめに見積もる必要がある。
- 動作確認用に、`dcm4che`（Java, Apache 2.0ライセンス）や `DCMTK`（C++, BSD類似ライセンス）といった、いずれも無料の既存OSS PACSツール群を「相手役」（テスト用のSCU/SCP）として使うと、自作実装との疎通確認がしやすい。

**アーキテクチャへの影響**

- 現在の「ブラウザからGraphQL Mutationへ直接HTTPアップロード」という流れが、「外部/自身のモダリティ（送信元）→ C-STORE → SCPリスナー（常駐プロセス）→ Temporalワークフロー起動（ストレージ保存＋DB登録）」という流れに変わる。
- SCP側は常時ポート待受が必要（ポート番号は要確認、DICOM標準のデフォルトは104番だが、通常は1024番以降の非特権ポート、例: 11112番などを使うことが多い）。
- 既存のGraphQL `uploadDicomFiles` Mutationを廃止するのか、DICOM通信の導入後も並行運用（ブラウザからの手動アップロード用に残す等）するのかは要確認。

## 5. 結論（現時点の見立て）

- 洗い出した要素はすべて**費用0円**で技術的に再現可能（VMware無料化・Windows Server 2025評価版・PostgreSQL/Nuxt/Blazor/.NET/Temporal/Swagger/fo-dicomがいずれも無料OSSのため）。
- **VM構築・PostgreSQL導入・メモリ増強・Webサーバー選定（IIS）は既に方針確定**であり、フェーズ1はIISセットアップと接続切り替えの実作業のみ。
- 「お金のコスト」はほぼゼロにできる一方、「時間・設計コスト」は要素ごとに差が大きい。特に工数がかかる想定：
  - **DICOM通信のSCU/SCP化（fo-dicom導入）**：今回追加された要件の中で最も技術難易度が高く、優先して検討すべき項目。
  - dicom-tool側のDB接続をSQLiteからPostgreSQL（`192.168.93.128`）へ切り替え。
  - Temporal（C# SDK、セルフホスト）導入と、アップロード/削除それぞれの「ストレージ操作」「DB操作」タスク分離のワークフロー化。
  - ワークリスト⇔常駐トレイアプリ（C#, HTTP）間の連携実装。
  - ワークリスト／タイムライン／Viewerを実務相当の3サービスに分ける設計（Viewerは既存実装の流用でコスト圧縮可）。

## 6. 進め方の選択肢（実装はまだしない）

DICOM通信の方式変更が入り口（データの受け取り方）を変える大きな変更のため、依存関係を踏まえて並べ直すと以下のようになります。

1. **フェーズ1**：IISセットアップ + dicom-tool側のDB接続をSQLiteからPostgreSQL（`192.168.93.128`）へ切り替え。— 基盤整備。
2. **フェーズ2**：fo-dicomによるDICOM通信プロトコル化（C-ECHO/C-STOREのSCU/SCP実装）。— 最も難易度が高いが、後続のワークフロー設計の起点になるため優先度高。
3. **フェーズ3**：Temporal（C# SDK、セルフホスト）導入 + アップロード/削除時の「ストレージ操作」「DB操作」タスク分離のワークフロー化（フェーズ2のSCP受信をトリガーとする）。
4. **フェーズ4**：常駐トレイアプリ（C#、HTTP常時待受）のプロトタイプ。
5. **フェーズ5**：ワークリスト／タイムライン／Viewerをそれぞれ独立サービスとして分離。
6. **フェーズ6**：任意のサービスへのSwagger（Swashbuckle/NSwag）導入。

※ 優先順位はヒアリング内容次第で今後も変わり得ます。

## 7. 追加で確認したいこと

- SCP（受信側）が待ち受けるポート番号、および自システムのAEタイトル。
- SCU（送信側）としてテストする際の送信先（他のPACS/モダリティを想定しているか、まずは自己ループバックでの疎通確認で良いか）。
- 既存のGraphQL `uploadDicomFiles` Mutation（HTTPマルチパート）は、DICOM通信導入後も並行運用するのか、廃止するのか。
- 将来的にC-FIND（検索）やC-MOVE（転送依頼）などの他のDIMSEサービスも必要になるか（ワークリスト取得等に関連しうる）。
- Timelineアプリ・Viewerアプリの起動関係（Timelineの中からViewerを開くのか、常駐アプリが個別に起動できるのか）。
- 全体のポート番号一覧（Webサーバー、常駐アプリのHTTP、DICOM SCP等、後日共有いただける由）。

---

[^1]: [VMware Fusion and Workstation are Now Free for All Users - VMware Cloud Foundation (VCF) Blog](https://blogs.vmware.com/cloud-foundation/2024/11/11/vmware-fusion-and-workstation-are-now-free-for-all-users/)
[^2]: [Download and license VMware Desktop Hypervisor (Fusion Pro and Workstation Pro) - Broadcom](https://knowledge.broadcom.com/external/article/368667/download-and-license-vmware-desktop-hype.html)
[^3]: [Temporal Cloud pricing | Temporal Platform Documentation](https://docs.temporal.io/cloud/pricing)
[^4]: [Windows Server 2025 | Microsoft Evaluation Center](https://www.microsoft.com/en-us/evalcenter/evaluate-windows-server-2025)
[^5]: [fo-dicom/License.txt at development - GitHub](https://github.com/fo-dicom/fo-dicom/blob/development/License.txt)
