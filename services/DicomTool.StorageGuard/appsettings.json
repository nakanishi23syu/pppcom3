{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  "StorageGuard": {
    "//_MinFreePercentDefault_コメント": "GET /capacity/check でminFreePercentクエリパラメータが省略された場合に使う既定の閾値(%)。Worker側(services/DicomTool.Worker/appsettings.json StorageGuard:BaseUrl)とは別の設定であることに注意。",
    "MinFreePercentDefault": 10.0
  }
}
